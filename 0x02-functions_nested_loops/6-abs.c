#include "main.h"

/**
 * _abs - _abs function
 *
 * Description:Compute absolute value
 *
 * @i:value to check
 *
 * Return:return between i or -i
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
